# Jerome

A contextual e-reader for language learning. Load an epub, click words, and generate Anki flashcards with context-aware definitions using LLMs.

## Development

```sh
pnpm install
pnpm dev
```

Built with SvelteKit, TypeScript, and Tailwind CSS.
